from django.urls import path, include
from . import views

urlpatterns = [
    # path('', views.about.as_view(), name='about'), # TODO make view
    # path('create-quiz/', views.CreateQuiz, name='create-quiz'),  # Add this path to create a new quiz instance
]